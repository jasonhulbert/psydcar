# Frontend

Angular 21 standalone app is planned for Phase 9.

Phase 0 intentionally keeps this directory as a placeholder so the repository layout is
stable without committing generated Angular workspace files before the backend API
contracts exist.
